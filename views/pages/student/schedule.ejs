<section class="page-grid">
  <div class="panel workspace-panel">
    <div class="section-head">
      <div>
        <h3 class="section-title">我的课表</h3>
      </div>
      <div class="section-actions">
        <a href="/student/campus-schedule" class="ghost-button">查看全校课表</a>
      </div>
    </div>

    <div class="schedule-scroll">
      <table class="schedule-table">
        <thead>
          <tr>
            <th>节次</th>
            <th>周一</th>
            <th>周二</th>
            <th>周三</th>
            <th>周四</th>
            <th>周五</th>
            <th>周六</th>
            <th>周日</th>
          </tr>
        </thead>
        <tbody>
          <% scheduleRows.forEach(function(row) { %>
            <tr>
              <th>
                <div><%= row.period.label %></div>
                <div class="muted-copy"><%= row.period.time %></div>
              </th>
              <% row.days.forEach(function(day) { %>
                <% if (day && day.skip) { %>
                <% } else if (day) { %>
                  <td rowspan="<%= day.span %>">
                    <div class="schedule-card">
                      <strong><%= day.course_name %></strong>
                      <div class="muted-copy"><%= day.section_code %></div>
                      <div class="muted-copy"><%= day.teacher_name %></div>
                      <div class="muted-copy"><%= day.building_name %> <%= day.room_number %></div>
                    </div>
                  </td>
                <% } else { %>
                  <td></td>
                <% } %>
              <% }) %>
            </tr>
          <% }) %>
        </tbody>
      </table>
    </div>
  </div>
</section>
